---
import SectionTitle from "../Text/SectionTitle/SectionTitle.tsx";
---

<section id="about" class="relative min-h-screen">
  <div
    class="about_wrapper absolute inset-0 pt-[var(--header-height)] overflow-hidden flex flex-col"
  >
    <SectionTitle client:visible title="Let me introduce myself . " />
    <q class="quote">Embrace challenges to predict and create your future.</q>
    <div class="details_wrapper">
      <p class="details_text">
        "I'm all about crafting web experiences that not only meet but exceed
        expectations. Collaboration and communication are at the heart of my
        work – it's about joining forces to bring visions to life.
      </p>
      <div class="separator w-[50%] h-0.5 my-12 bg-[var(--color-secondary)]">
      </div>
      <p class="details_text">
        Beyond the screen, I'm an avid traveler, continuously drawn to new
        countries and cultures. The comforting aroma of coffee and the soothing
        sounds of music inspire my personal journey."
      </p>
    </div>
  </div>
</section>

<style lang="scss">
  .quote {
    font-family: var(--paragraph-font-family);
    font-size: 1.5rem;
    font-style: italic;
    font-weight: 200;
    align-self: flex-end;
    padding: 1rem;
    color: var(--color-secondary);
  }

  .details_wrapper {
    flex-grow: 1;
    padding-inline: 20vw;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
    align-items: center;
  }
  .details_text {
    font-family: var(--paragraph-font-family);
    max-width: 80ch;
    font-size: 2rem;
    line-height: 1.3;
    letter-spacing: -0.1rem;
    font-weight: 200;
    padding: 1rem;
    color: var(--color-secondary);
  }
  .separator {
    background-color: var(--color-secondary);
  }
</style>

<script>
  /*   import simpleParallax from "simple-parallax-js";
  const image = document.querySelector(".image");

  if (image !== null) {
    new simpleParallax(image, {
      orientation: "right",
    });
  } */
</script>
